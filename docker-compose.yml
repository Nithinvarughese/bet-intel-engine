services:
  db:
    image: postgres:15
    container_name: football_db
    restart: always
    environment:
      POSTGRES_USER: nithin_dev
      POSTGRES_PASSWORD: password123
      POSTGRES_DB: football_analytics
    ports:
      - "5432:5432"
    volumes:
      - ./postgres_data:/var/lib/postgresql/data